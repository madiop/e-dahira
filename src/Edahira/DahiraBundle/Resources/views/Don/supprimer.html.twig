{% extends "EdahiraDahiraBundle::layout.html.twig" %}

{% trans_default_domain 'EdahiraDahiraBundle' %}

{% block title %}{{ parent()}} | {{'label.don' | lower | trans }}{% endblock %}

{% block body_header %}
	<h1>{{ 'text.suppression' | trans({'%objet%': 'dépense.'}) }}</h1>
{% endblock %}

{% block body_content %}
	{{ parent() }}
	<form class="form-horizontal" action="{{ path('don_supprimer',{'idType': 20, 'type': 1, 'id': don.id}) }}" method="post" >
		<fieldset>
	        <div class="form-actions">
				<input type="submit" class="btn btn-success btn-large" value={{ 'label.supprimer'| trans}} />
				<a class="btn" href="{{ path('don_index', {'idType': 20, 'type': 1, 'id': don.id}) }}">{{ 'label.annuler'| trans}}</a>
			</div>
		</fieldset>
	</form>
{% endblock %}